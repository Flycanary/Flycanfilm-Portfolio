import React from 'react';

// Placeholder for Sound Context
// This context could manage ambient sound effects for the portfolio.
export const SoundContext = React.createContext({ isSoundEnabled: false, toggleSound: () => {} });

// A provider component can be created here if needed in the future.
