import React from 'react';

// Placeholder for Mouse Context
// This context can be used to track mouse position across the application.
export const MouseContext = React.createContext({ x: 0, y: 0 });

// A provider component can be created here if needed in the future.
